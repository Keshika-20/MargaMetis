[build]
builder = "dockerfile"
dockerfilePath = "backend/Dockerfile"
dockerContext = "."

[deploy]
startCommand = "gunicorn -w 1 -b 0.0.0.0:$PORT --timeout 120 wsgi:app"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
